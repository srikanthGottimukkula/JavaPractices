package practice;


import javaAIT.Student;

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

public class ListToMapConversion {
    public static void main(String[] args) {

        List<String> list = Arrays.asList("srikanth", "ramesh", "ali", "joe");

//        Map<Integer, String> collect = list.stream().collect(Collectors.toMap(Student::getSno, Student::getsName));


    }
}
